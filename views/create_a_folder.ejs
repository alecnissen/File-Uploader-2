<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <title>Create a folder</title>
</head>
<body class="create-folder-body-styles">
    
    <h1 class="create-folder-header-styles">Create A Folder</h1>

    <form method="post" action="/create_a_folder" class="name-folder-styles">
        <label>Name Your Folder</label>
        <input type="text" name="folderName" required />
        <button type="submit" class="name-folder-btn-styles">Submit</button>
    </form>

    <br>

    <button class="create-folder-back-to-home-button-styles">
    <a href="/">Back to home</a>
    </button>

    <br>
      
      <% if (messages && messages.length > 0) { %>
          <div id="message-styles"><%= messages[0] %></div>
      <% } %>

      <script> 

        const messageStyles = document.getElementById('message-styles');
        if (messageStyles) {
          setTimeout(() => {
            messageStyles.style.display = 'none';
          }, 3000);
        }

      </script>

</body>
</html>